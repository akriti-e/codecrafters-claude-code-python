# codecrafters-claude-code-python
A test-driven CLI agent built via CodeCrafters. Employs an LLM tool-calling loop to execute local file operations and shell scripts inside an interactive terminal REPL.
